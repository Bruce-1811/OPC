# opc-web

OPC（One Person Company）前端：React + Vite + TypeScript + Ant Design Mobile。

## 开发

```bash
npm install
npm run dev
```

默认地址：http://localhost:5173（`/api` 代理到后端 `8080`）。

## 构建

```bash
npm run build
```

产物在 `dist/`，可由 Nginx 托管。
