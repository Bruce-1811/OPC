# OPC 双人合作指南

> 目标：把**剩余阶段**拆给两个人，尽量**文件不抢、接口边界清晰、联调点明确**。  
> 依据：[`opc开发/OPC-项目开发大纲.md`](./opc开发/OPC-项目开发大纲.md)、[`opc开发/OPC-API接口清单.md`](./opc开发/OPC-API接口清单.md)、当前 `One-Person-Campany` 代码现状。

---

## 1. 先对齐：现在做到哪了

| 阶段 | 状态 | 说明 |
|------|------|------|
| 0 搭架子 | ✅ | 前后端可跑、5 Tab、健康检查 |
| 1 登录用户 | ✅ | 注册/登录/JWT/我的/编辑资料 |
| 2 发布发现 | ✅ | 发布/草稿/发现/详情 |
| 3 收藏申请 | ✅ 基本完成 | 收藏、申请、审核、我发出的/待我审核；细节可小修 |
| **4 项目执行** | ⏳ 进行中 | 后端已有 `projects/mine`、tasks 雏形；**前端项目 Tab 仍是空壳** |
| **5 消息** | ⏳ 未开始 | 无会话接口、消息页占位 |
| **6 问培风** | ⏳ 未开始 | 无 AI 接口、无助手页 |
| **7 联调收尾** | ⏳ 未开始 | 全链路 Demo |

**两人从阶段 4 起正式并行。** 阶段 3 若还有小 bug，谁碰到谁修，或每人固定半天「阶段 3 收尾」后进入主线。

---

## 2. 角色划分（建议直接采用）

下面用 **同学甲 / 同学乙** 称呼，你们把名字填进第二节表格即可。

| 同学 | 主责模块 | 一句话 |
|------|----------|--------|
| **甲** | **阶段 4：项目 Tab + 任务** | 让「申请通过后」能在项目页看到进行中项目、建任务、勾待办 |
| **乙** | **阶段 5：消息聊天** | 让成员能建会话、列表聊天、详情「联系发布人」真正可用 |
| **甲 + 乙** | **阶段 6～7** | 见下文：6 按前后端拆，7 一起联调 |

### 为什么这样拆（冲突小）

| | 甲（项目/任务） | 乙（消息） |
|--|----------------|------------|
| 主要表 | `projects`、`project_members`、`project_tasks` | `conversations`、`conversation_users`、`messages` |
| 主要后端文件 | `routes/projects.ts`、`routes/tasks.ts`、相关 service | **新建** `routes/conversations.ts`（或 `messages.ts`） |
| 主要前端文件 | `pages/ProjectPage.tsx`、**新建** `api/tasks.ts`、项目相关组件 | `pages/MessagePage.tsx`、**新建** 聊天页、`api/conversations.ts` |
| 页面入口 | 底部「项目」Tab | 底部「消息」Tab + 详情「联系发布人」 |

两边几乎不改同一文件；唯一需要约定的是：**申请通过后是否自动建项目群**（见第 5 节接口缝）。

---

## 3. 把名字写在这里

| 代号 | 真实姓名 | GitHub / 备注 |
|------|----------|----------------|
| 甲 | （填写） | 主责阶段 4 |
| 乙 | （填写） | 主责阶段 5 |

建议分支命名：

- 甲：`feature/4-project-tasks`
- 乙：`feature/5-messages`
- 阶段 6：`feature/6-ai-assistant`（谁主做谁开，或再拆 `6-ai-backend` / `6-ai-frontend`）
- 集成：合并到 `dev`，稳定后再进 `main`

---

## 4. 详细任务清单

### 4.1 同学甲 —— 阶段 4（项目执行）

**目标：** 底部「项目」Tab 可用；成员能看到自己的项目与待办。

#### 后端（在已有基础上补齐）

对照 API 清单「项目 / 任务」章节，优先：

| 优先级 | 接口 | 说明 |
|--------|------|------|
| P0 | `GET /projects/mine?status=` | 已有雏形 → 对齐清单字段与分页 |
| P0 | `GET /projects/published` | 我发布的项目（我的页入口） |
| P0 | 任务 CRUD 系列 | `POST/GET .../tasks`、`GET /tasks/mine`、`PATCH /tasks/:id`（已有雏形 → 对齐响应） |
| P1 | `PATCH /projects/:id` | 改 progress / status（已有雏形 → 与前端项目页对齐） |

**尽量只动：**

- `opc-server/src/routes/projects.ts`（`mine` / `published` / patch）
- `opc-server/src/routes/tasks.ts`
- 如需：`services/*` 中与项目查询相关的文件  
- **不要大改** `applications.ts` / `favorites.ts` / 新建消息路由

#### 前端

| 优先级 | 页面/文件 | 说明 |
|--------|-----------|------|
| P0 | `pages/ProjectPage.tsx` | 进行中 / 待确认 / 已完成 / 已归档 + 待处理任务 |
| P0 | **新建** `api/tasks.ts` | 封装任务接口 |
| P0 | `api/projects.ts` | 增加 `fetchMyProjects` / `fetchPublished` 等 |
| P1 | 我的页入口「已发布」 | 列表页可新建 `PublishedPage.tsx` 或先做简单 List |
| P1 | 项目内任务列表 UI | 可在详情或项目页二级展开 |

**原型：** `opc介绍/.../prototypes/project.png`

#### 甲的验收（自己先过一遍）

1. A 审核通过 B 后，B 在「项目 → 进行中」能看到该项目  
2. 能创建任务 →「待处理」出现 → 标记完成  
3. 按状态 Tab 过滤正确  

---

### 4.2 同学乙 —— 阶段 5（消息）

**目标：** 消息 Tab 有会话列表与聊天；详情「联系发布人」不再是 Toast。

#### 后端（建议整模块新建，减少撞车）

| 优先级 | 接口 | 说明 |
|--------|------|------|
| P0 | `GET /conversations` | 当前用户会话列表 |
| P0 | `GET /conversations/:id/messages` | 历史消息（可分页） |
| P0 | `POST /conversations/:id/messages` | 发文字；更新 last_message / 未读 |
| P0 | `POST /conversations/:id/read` | 清未读 |
| P1 | `POST /conversations` | 创建私聊/项目群（联系发布人） |

**建议新建文件（乙独占）：**

- `opc-server/src/routes/conversations.ts`（或 `messages.ts`）
- 在 `routes/index.ts` **只加一行挂载**（改 index 前先和甲说一声，或甲先合并后再挂）

**表：** `conversations`、`conversation_users`、`messages`（已在 sql / Prisma 中，一般不用改表结构）

**第一版策略：** 列表轮询（5～10s）即可，不上 WebSocket。

#### 前端（乙独占页面）

| 优先级 | 文件 | 说明 |
|--------|------|------|
| P0 | `pages/MessagePage.tsx` | 会话列表 + 未读角标 |
| P0 | **新建** `pages/ChatPage.tsx`（路径自定，如 `/message/:id`） | 气泡 + 发送 |
| P0 | **新建** `api/conversations.ts` | 封装消息接口 |
| P0 | `App.tsx` | 增加聊天路由（改动很小，合并时注意冲突） |
| P1 | `ProjectDetailPage.tsx` 的「联系发布人」 | 从 Toast 改为跳转/创建会话（**只改这一处按钮逻辑**，合并前和甲同步） |

**原型：** `message.png`、`nav-message.png`

#### 乙的验收

1. 两用户同项目群（或私聊）能互发文字，列表能刷出最后一条  
2. 未读数进入聊天后清零  
3. 详情「联系发布人」能进到会话  

---

### 4.3 阶段 6：问培风（在 4、5 可演示后再做）

为继续降低冲突，建议：

| 同学 | 负责 |
|------|------|
| **乙** | 后端 `POST /ai/chat`、`GET /ai/chat/history` + 表 `ai_chats`；新建 `routes/ai.ts` |
| **甲** | 前端问培风页 + 悬浮入口；调用 AI 接口；快捷问题「今天要处理什么」对接甲已做好的 `/tasks/mine` |

第一版允许 **Mock 回复**（模板 + 待办列表），不强制接大模型。

**依赖：** 甲的任务接口最好先可用，否则「今天要处理什么」只能写死文案。

---

### 4.4 阶段 7：一起收尾（两人共同）

| 项 | 做法 |
|----|------|
| 全链路走查 | 两人各用一套账号互测（发布者 / 申请者） |
| Bug 清单 | 建一个共享文档或 Issue，标 P0/P1 |
| UI 贴原型 | 甲盯项目/发现/发布；乙盯消息/问培风；我的页两人各修自己引入的入口 |
| 文档 | 更新根 `README.md` 进度表；接口有变同步 `opc开发/` |
| Demo | 录一条主路径视频：发现→申请→审核→项目任务→消息→问培风 |

---

## 5. 减少冲突的硬约定

### 5.1 文件所有权（默认谁主责谁改）

| 区域 | 默认负责人 |
|------|------------|
| `ProjectPage.tsx`、`api/tasks.ts`、tasks 路由 | 甲 |
| `MessagePage.tsx`、聊天页、`api/conversations.ts`、conversations 路由 | 乙 |
| `routes/index.ts`、`App.tsx` | **小改 + 先拉再推**；同一天改就语音对一下 |
| `ProjectDetailPage.tsx` | 甲一般不动；乙只改「联系发布人」；若甲也要加任务入口，分两天合并 |
| `applications.ts` / 收藏相关 | 阶段 3 已完成，**默认冻结**，除非修 bug |
| `opc开发/*.md` | 谁改接口谁更新对应段落，PR 里写明 |

### 5.2 接口缝（唯一容易打架的点）

**申请审核通过时，要不要自动创建项目群？**

建议约定（二选一，开干前定死）：

| 方案 | 做法 | 推荐 |
|------|------|------|
| **A（推荐）** | 乙提供「创建/获取项目会话」接口；甲或乙在 `PATCH /applications/:id` 通过分支里 **调用乙的 service 函数**（不要复制粘贴建群逻辑） | ✅ |
| B | 阶段 5 先不做自动建群；用户点「联系发布人」再建；申请通过只加成员 | 更简单，Demo 也能讲 |

定案后写在本文件这里：

> **当前约定：** ________________（A / B）

### 5.3 联调节奏

1. **各自先用 mock / Apifox 把本模块跑通**  
2. 每完成一个可演示切片就合并到 `dev`（不要攒一周）  
3. **阶段 4 与 5 都进 `dev` 后**，安排半天双人联调：  
   `申请通过 → 项目页有项目 → 建任务 → 发消息`  
4. 再开阶段 6  

### 5.4 契约优先

- 新接口先在 [`OPC-API接口清单.md`](./opc开发/OPC-API接口清单.md) 补一小节（路径、请求、响应示例）  
- 统一响应：`{ code, message, data }`，成功 `code === 0`  
- 需要登录的接口：`Authorization: Bearer <token>`  

---

## 6. 建议时间线（可按课表改）

| 周次 | 甲 | 乙 | 共同 |
|------|----|----|------|
| 第 1 周 | 项目 Tab + mine/任务联调 | 会话列表 + 发消息后端 | 对齐「申请通过是否建群」 |
| 第 2 周 | 待办/进度/已发布列表 | 聊天页 + 联系发布人 | 合并到 `dev`，走通 4+5 |
| 第 3 周 | 问培风前端 + 待办快捷问 | AI 后端 Mock | 阶段 7 全链路 + Demo |

---

## 7. 每日协作最小闭环

1. 开工前：`git pull` 目标分支（建议 `dev`）  
2. 只在自己主责文件里大改  
3. 提交信息写清模块：如 `feat(project): 项目 Tab 待办列表` / `feat(message): 发送消息接口`  
4. 改了接口：同步改 `opc开发` 文档  
5. 卡住超过半天：优先问对方「是不是在等我的接口」，而不是闷头改对方文件  

---

## 8. 一页纸分工速查

```text
甲：项目 Tab + 任务（阶段 4）→ 问培风前端（阶段 6 前半）
乙：消息会话 + 聊天（阶段 5）→ 问培风后端（阶段 6 后半）
共同：阶段 7 联调 / Demo / README 进度
冻结：阶段 0～3 主链路（只修 bug）
```

---

## 9. 相关文档

| 文档 | 用途 |
|------|------|
| [README.md](./README.md) | 总入口与进度 |
| [opc开发/OPC-项目开发大纲.md](./opc开发/OPC-项目开发大纲.md) | 阶段任务与验收 |
| [opc开发/OPC-API接口清单.md](./opc开发/OPC-API接口清单.md) | 接口契约 |
| [opc开发/OPC后端数据库设计.md](./opc开发/OPC后端数据库设计.md) | 表结构 |
| [One-Person-Campany/README.md](./One-Person-Campany/README.md) | 本地启动 |
| [opc介绍/](./opc介绍/) | 原型与产品说明 |

---

*文档版本：v1.0 | 按当前仓库进度编写：阶段 3 已完成，剩余从阶段 4 起双人并行*
